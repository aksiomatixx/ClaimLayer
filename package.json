{
  "name": "homecare-tpa",
  "version": "0.0.0",
  "private": true,
  "description": "HomeCare TPA monorepo orchestrator. Backend, frontend, and demo seed scripts.",
  "scripts": {
    "dev:demo": "NODE_ENV=development node scripts/dev-demo.js",
    "db:reset": "NODE_ENV=development node scripts/db-reset.js",
    "seed:demo": "NODE_ENV=development node backend/src/scripts/seedDemo.js",
    "dev": "node scripts/dev.js",
    "test": "cd backend && npm test"
  }
}
